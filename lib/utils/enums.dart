enum APIMode { dev, test, prod }

enum PickType { image, media, video }

enum ButtonType { raised, text, border }

enum AlertGroupType { all, group, specific }

enum FieldType { checkBox, dropDown, radio, text }

enum TransitionType { normal, fade, scale, slide, size, rotate }

enum HTTPMethodType { delete, download, fetch, head, patch, post, put }

enum LoaderType {
  chasingDots,
  circle,
  cubeGrid,
  dancingSquare,
  doubleBounce,
  dualRing,
  fadingCircle,
  fadingCube,
  fadingFour,
  fadingGrid,
  foldingCube,
  hourGlass,
  normal,
  pianoWave,
  pouringHourGlass,
  pouringHourGlassRefined,
  pulse,
  pulsingGrid,
  pumpingHeart,
  ring,
  ripple,
  rotatingCircle,
  rotatingPlain,
  spinningCircle,
  spinningLines,
  squareCircle,
  threeBounce,
  threeInOut,
  wanderingCubes,
  wave,
  waveSpinner
}
